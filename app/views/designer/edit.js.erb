
(function() {
  // $('#modal-content').change({
  //   // form = querySelector('#edit_resource_1');
  //   console.log("stuff");
  // });
  $('#modal-content').html("<%= escape_javascript(render partial: 'resource_form') %>");
  // code = document.getElementById('modal-content');
  // code.innerHTML = "<%= escape_javascript(render partial: 'resources/form') %>";

  // reload resource functions after Modal is opened
  var ready;
  ready = function () {
    prepareResourceFunctions();
    prepareDesignerFunctions();

    // form.setAttribute('data-remote', 'true');
    // console.log("stuff");

    // console.log("stuff");
    // var submit;
    // console.log("stuff");
    // submit = document.querySelector("div.submit-button < input");
    // console.log("stuff");
    // console.log(submit);
  };

  $(document).ready(ready);
  // $(document).on('page:load', ready);

}).call(this);

// console.log("stuff");
// var form;
// form = querySelector('#edit_resource_1');
// console.log("stuff");
